using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Ads_Initialization_Scene : MonoBehaviour
{
    
    // Start is called before the first frame update
    void Start()
    {
        /*// Find the GameObject holding the BannerView from the first scene
        GameObject myGameObject = GameObject.Find("myBannerAdObject");

        // Get the BannerWrapper component from the GameObject
        BannerWrapper bannerWrapper = myGameObject.GetComponent<BannerWrapper>();

        // Hide the banner ad in the second scene
       // bannerWrapper.HideBanner();

        bannerWrapper.ShowBanner();*/
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
